import React, { Component } from 'react';
import Score from './score';

export default class App extends Component {
    render() {
        return (
            <div>
                <Score />
            </div>
        );
    }
}
